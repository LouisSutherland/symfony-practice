{% extends 'base.html.twig' %}

{% block body %}
    {% if not table.data %}
        No records to show.
    {% else %}
        {{ include('table.twig', {'table': table}) }}
    {% endif %}
{% endblock %}